# 复制此文件为 keystore.properties 后填真实值，不要把 keystore.properties 提交到仓库
storeFile=../keystore/release.keystore   # 或绝对路径
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
