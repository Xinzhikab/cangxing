# 收藏app — PRD v1.0（定稿）

> 本文档是开发指令，直接喂给 Codex/Trae/Cursor 等 AI IDE 即可开工。
> 原始想法背景见 `IDEA.md`（含页面交互细节与状态机）。
> **状态：2026-08-13 定稿**（功能范围、数据模型、技术栈均已确认）。

---

## 1. 项目一句话

一个手机上的**统一收藏夹**：把抖音/酷安/小黑盒等平台的**文章和评论**，通过系统分享菜单一键保存到本地，**AI 自动转录排版**，**文件夹式分类**，支持**全文搜索**、**整篇笔记**、**回顾提醒**。

## 2. 定位与边界

- 个人自用工具，单用户，**无账号/登录体系**
- 平台：iOS + Android（一套代码双端）
- AI：**纯 LLM API** 转录排版（内容为公开文章，无隐私顾虑）
- 数据：**本地优先**，存储格式可迁移（为将来 NAS/网盘同步留路）
- **本版明确不做**：多用户、社交分享、视频转录、应用商店上架、语义搜索

## 3. MVP 功能清单（本版必做）

### 采集
1. 系统分享菜单入口（iOS Share Extension / Android ACTION_SEND），接收链接或文本
2. 类型选择：**文章 or 评论**（可智能预判、可手动改）
3. 手动粘贴链接/文本（兜底入口，与分享共用保存页）
4. **Onboarding 引导页**：首次启动教"分享→保存"三步

### AI 转录
5. LLM 自动识别链接内容、转录成排版好的正文（Markdown）
6. 自动提取元数据：标题、作者、原帖时间、平台
7. 图片识别并下载到本地
8. 转录失败：错误提示 + 重试 + "仅存原文"兜底（**失败不阻塞保存**）

### 存储
9. 本地存储，原帖删除后仍可完整阅读
10. 数据格式：Markdown 正文 + JSON 元数据 + 本地图片目录（见 §6）

### 组织与查找
11. 文件夹式多级分类（新建/重命名/移动/合并，类似 Windows 资源管理器）
12. 搜索：标题 + 全文（输入即搜，结果高亮）
13. 筛选：按平台 / 作者 / 状态

### 阅读与笔记
14. 排版正文阅读视图
15. 整篇笔记（每篇收藏一条，可编辑，再次查看可见）
16. 跳转原帖链接

### 回顾
17. 状态标记：未读 / 已读 / 想学(learning) / 已完成(done)
18. **想学 → 回顾队列**：到期本地通知提醒翻看；"再学一次"重新排期

## 4. 核心用户流程

```
任意app点分享 → 选本app → 选[文章/评论] → 点保存
     ↓                            ↓失败
LLM 转录排版(≤5s)             [重试] [仅存原文]（不阻塞）
     ↓
归入文件夹（默认"未分类"）→ 完成
     ↓ 随时
搜索/翻看 → 加整篇笔记 → 标"想学"(选节奏) → 到期提醒 → 翻看 → 标"已完成"
```

## 5. 页面清单

| 页面 | 要点 |
|---|---|
| **Onboarding**（首次启动） | 3 页左右：介绍价值 → 演示"分享→保存" → 进入 |
| **收藏页**（默认 Tab） | 顶部搜索栏；筛选行（平台/作者/状态/排序）；文件夹树 + 文章列表；长按管理；右上角"+"手动添加 |
| **保存页**（分享/手动共用） | 内容预览；类型选择；分类选择；[☐ 仅存原文稍后转录]；保存按钮；转录进度（抓取→转录→图片）；失败操作条 |
| **阅读页** | 标题/作者/平台/时间；Markdown 正文；底部操作栏：笔记、想学、分类、删除；原帖跳转；从"想学"进入时显示"已读完？标完成" |
| **想学 Tab** | 回顾队列按到期时间分组（已到期→今天→3天内→之后）；操作：去翻看/再学一次/已完成；全局节奏设置 |
| **设置 Tab** | LLM API Key + 模型 + Base URL；存储统计；导出备份/导入；回顾开关与默认间隔 |

## 6. 数据模型

### 6.1 存储布局（本地，可整体复制迁移）

```
收藏根目录/
├── meta/                    # 每篇一个 JSON 元数据
│   └── {id}.json
├── content/                 # 每篇一个 Markdown 正文
│   └── {id}.md
└── images/                  # 转录时下载的图片
    └── {id}/img_001.jpg ...
```

### 6.2 元数据 JSON Schema

```json
{
  "id": "uuid",
  "title": "标题",
  "type": "article | comment",
  "source_platform": "douyin | xiaoheihe | coolapk | other",
  "source_url": "原帖地址",
  "author": "作者",
  "published_at": "原帖时间 ISO8601 或 null",
  "collected_at": "收藏时间 ISO8601",
  "category": ["父级", "子级"],
  "images": ["images/{id}/img_001.jpg"],
  "note": "整篇笔记（可为空）",
  "status": "unread | read | learning | done",
  "review_due_at": "下次回顾时间 ISO8601 或 null",
  "raw_input": "原始文本/链接（兜底保留，便于重新转录）"
}
```

### 6.3 分类

- 多级文件夹，支持任意深度；"未分类"为默认根项
- 分类信息存于元数据 `category` 数组，避免依赖目录结构

## 7. AI 转录接口设计

### 7.1 输入

- 链接模式：客户端抓取网页正文（尽力而为，抓不到则降级为粘贴模式）
- 粘贴模式：用户直接粘贴全文文本
- 快速收藏：仅存 `raw_input`，不调用 LLM，阅读时再转录

### 7.2 LLM 调用（OpenAI 兼容格式）

```
system: 你是内容转录助手。把用户提供的文章/评论转录为排版好的 Markdown，
        去除导航/广告/无关内容，并提取元数据。
        只输出 JSON：{"title","content_md","author","platform","published_at"}
user: [原始文本]
```

- 要求 LLM **只输出合法 JSON**（便于解析）
- 图片：Markdown 中保留原图 URL，由客户端逐个下载到 `images/{id}/` 并替换为本地路径
- 超时/解析失败 → 走"仅存原文"兜底

### 7.3 关键兜底原则

**转录是增强，保存是底线**：任何失败路径都必须保证原文先落盘（`raw_input` 字段），用户随时可重新转录。

## 8. 技术栈（已确认 2026-08-13）

| 项 | 选定 | 说明 |
|---|---|---|
| 跨端框架 | **Flutter** | 单代码双端；本地存储/文件系统生态成熟 |
| 本地数据库 | SQLite + FTS5 | 元数据查询 + 标题/全文搜索（中文分词方案开工时验证） |
| Markdown 渲染 | flutter_markdown 等 | 阅读页渲染正文 |
| 本地通知 | flutter_local_notifications | 回顾提醒 |
| 分享扩展 | iOS Share Extension / Android ACTION_SEND | 平台原生能力，Flutter 侧用插件桥接 |
| LLM API | OpenAI 兼容接口（Base URL 可配，兼容国内中转） | 一套代码适配 Claude/GPT/中转 |

## 9. 验收标准（MVP 完成判定）

1. 抖音/酷安等 app 点分享 → 本 app 出现在菜单 → 进入保存页
2. 粘贴一篇文本 → 选"文章" → 保存 → ≤5s 得到排版正文 + 元数据 + 入库
3. 图片成功下载到本地，阅读页正常显示；原帖删除不影响阅读
4. 新建两级文件夹，把收藏移入，重启 app 后分类不丢
5. 搜索正文中的关键词能命中并高亮
6. 给收藏写笔记 → 保存 → 重新打开仍可见
7. 标"想学"选 1 天 → 到期收到本地通知 → 点击直达阅读页
8. 断网/LLM 失败 → "仅存原文"生效，数据不丢
9. 整个收藏根目录复制到另一台设备可正常打开（迁移验证）
10. 首次启动出现 Onboarding，走完三步进入主界面

## 10. 迭代路线（本版之后）

- **MVP+**：快捷收藏（已含于 MVP 入口）、回顾节奏细化、按平台/作者筛选增强
- **Phase 2**：存量批量导入（2000-3000 篇）、视频转录、NAS/网盘同步、片段级高亮批注、语义搜索、日历联动（可选）
